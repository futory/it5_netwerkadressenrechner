package it5.p04.fileadapter;

/**
 * Enum that distinguishes between HOST, BROADCAST, NETID, MASK, NAME
 * is used to define IPv4HostAddress, IPv4NetworkID, IPv4BroadcastAddress, IPv4SubnetMask and SubnetName
 *
 * Created by martin on 15/05/17.
 */
public enum Type {
    HOST, BROADCAST, NETID, MASK, NAME
}
